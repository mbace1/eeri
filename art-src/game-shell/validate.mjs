import{TITLE,RESUME,LEVEL_SELECT,validateShell}from'./shell-spec.js';let p=0,f=0;const ok=(n,c)=>{c?p++:f++;console.log(`${c?'ok':'FAIL'} ${n}`)};ok('spec valid',!validateShell().length);ok('no map',!LEVEL_SELECT.map);ok('12 addresses',LEVEL_SELECT.worlds*LEVEL_SELECT.levelsPerWorld===12);ok('continue primary',TITLE.primaryReturning==='continue');ok('resume uses slug',RESUME.usesCanonicalSlug);ok('no mid-jump save promise',!RESUME.arbitraryMidLevelSnapshot);console.log(`${p} passed, ${f} failed`);process.exit(f?1:0);