# Meshy — see `/ART_PIPELINE.md`

This file used to hold the Meshy inventory and the "Meshy feature first" rule.
Both now live in the repo-root **`ART_PIPELINE.md`**, which is canonical and
covers the whole method: concept → mesh → rig → animate → integrate, the
verification steps, the costs and the trap index.

Kept here only because links point at it. Do not re-grow a second copy — two
pipeline docs drift, and this one is about a pipeline that was rebuilt three
times from scattered notes.
