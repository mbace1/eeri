import{MACHINES,PRESENTATION,FUTURE_RIDE_CONTRACT,validateMachineControls}from'./control-spec.js';let p=0,f=0;const ok=(n,c)=>{c?p++:f++;console.log(`${c?'ok':'FAIL'} ${n}`)};ok('two proven machines',Object.keys(MACHINES).length===2);ok('spec valid',!validateMachineControls().length);ok('one contextual action max',PRESENTATION.maxContextActions===1);ok('does not block input',!PRESENTATION.blocksInput);ok('future rides stay simple',FUTURE_RIDE_CONTRACT.maxContextActions===1);console.log(`${p} passed, ${f} failed`);process.exit(f?1:0);