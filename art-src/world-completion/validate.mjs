import{BUILDING,CLOCK_OUT,SKYLINE,visibleParts,validateCompletion}from'./completion-spec.js';let p=0,f=0;const ok=(n,c)=>{c?p++:f++;console.log(`${c?'ok':'FAIL'} ${n}`)};ok('spec valid',!validateCompletion().length);ok('nine parts',BUILDING.parts===9);ok('4 means four visible',visibleParts(4).length===4);ok('nothing gates next world',!BUILDING.gatesNextWorld&&CLOCK_OUT.nextWorldAlwaysOpens);ok('building persists',BUILDING.persists);ok('returns in skyline',SKYLINE.carriesForward);console.log(`${p} passed, ${f} failed`);process.exit(f?1:0);