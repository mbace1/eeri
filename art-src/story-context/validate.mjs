import{WORLD_CONTEXT,COPY,STORY_RULES,validateStory}from'./story-spec.js';let p=0,f=0;const ok=(n,c)=>{c?p++:f++;console.log(`${c?'ok':'FAIL'} ${n}`)};ok('spec valid',!validateStory().length);ok('four worlds',Object.keys(WORLD_CONTEXT).length===4);ok('three language drafts',Object.keys(COPY).length===3);ok('no dialogue scenes',!STORY_RULES.dialogueScenes);ok('no timer',!STORY_RULES.timers);ok('visual story',STORY_RULES.visualCarriesStory);console.log(`${p} passed, ${f} failed`);process.exit(f?1:0);