import{PAUSE_ACTIONS,OPTIONS,A11Y,validateOptions}from'./options-spec.js';let p=0,f=0;const ok=(n,c)=>{c?p++:f++;console.log(`${c?'ok':'FAIL'} ${n}`)};ok('spec valid',!validateOptions().length);ok('continue first',PAUSE_ACTIONS[0].id==='continue');ok('restart is checkpoint restart',PAUSE_ACTIONS.some(x=>x.id==='restart_checkpoint'));ok('44px floor',A11Y.minTouchPx>=44);ok('three languages',OPTIONS.language.values.length===3);ok('motion follows system by default',OPTIONS.motion.default==='system');console.log(`${p} passed, ${f} failed`);process.exit(f?1:0);