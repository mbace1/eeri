import{SUMMARY,REPLAY,SECRET_HINT,validateReplay}from'./replay-spec.js';let p=0,f=0;const ok=(n,c)=>{c?p++:f++;console.log(`${c?'ok':'FAIL'} ${n}`)};ok('spec valid',!validateReplay().length);ok('next remains primary',SUMMARY.primary==='next_level');ok('no grades',!REPLAY.grades&&!REPLAY.stars);ok('no timer',!REPLAY.timer);ok('perfect stamp gates nothing',REPLAY.perfectStamp.gatesNothing);ok('hints have no coordinates',!SECRET_HINT.givesCoordinates);console.log(`${p} passed, ${f} failed`);process.exit(f?1:0);