import{PERSONALITY,SHARED,validatePersonality}from'./personality-spec.js';let p=0,f=0;const ok=(n,c)=>{c?p++:f++;console.log(`${c?'ok':'FAIL'} ${n}`)};ok('four existing kinds',Object.keys(PERSONALITY).length===4);ok('spec valid',!validatePersonality().length);ok('not angry',SHARED.angry===false);ok('gameplay untouched',SHARED.personalityChangesGameplay===false);ok('all kinds guard gameplay values',Object.values(PERSONALITY).every(x=>x.forbid.length));console.log(`${p} passed, ${f} failed`);process.exit(f?1:0);