# Feel Lab stack note

This branch intentionally stacks on PR #242 (`agent/eeri-feel-library`). Merge the feel-library source PR first; this lab then becomes a small, isolated follow-up containing only the audition UI and its source gate.
