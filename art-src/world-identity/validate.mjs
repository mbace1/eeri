import{WORLDS,IDENTITY_RULES,validateWorlds}from'./worlds.js';let p=0,f=0;const ok=(n,c)=>{c?p++:f++;console.log(`${c?'ok':'FAIL'} ${n}`)};ok('four worlds',Object.keys(WORLDS).length===4);ok('spec valid',!validateWorlds().length);ok('worlds use different motif signatures',new Set(Object.values(WORLDS).map(w=>w.motifs[0])).size===4);ok('not colour-only',IDENTITY_RULES.colourAloneNeverSufficient);ok('craft first',IDENTITY_RULES.craftedMaterialFirst);console.log(`${p} passed, ${f} failed`);process.exit(f?1:0);