# Machine concept binary import status

The following approved binary files are prepared locally for this PR but are not yet committed by the GitHub connector:

- `machine-roster-dark-eyes-approved.png`
- `machine-roster-toko-face-approved.png`

Reason: this connector can author repository text and Git objects but does not accept a local binary file path directly. The pack and SHA-256 hashes make the handoff deterministic.
